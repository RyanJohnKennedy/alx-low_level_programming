#include "main.h"

/**
 * print_most_numbers - print numbers but 2 and 4
 *
 * Return: void
 */

void print_most_numbers(void)
{
	int a;

	for (a = '0'; a <= '9'; c++)
	{
		if (a != '2' && c != '4')
			_putchar(a);
	}
	_putchar('\n');
}
