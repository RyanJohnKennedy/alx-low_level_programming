#include <stdio.h>

/**
 * main - prints its name
 * @argc: argument number
 * @argv: argument array
 * Return: 0
 */

int main(int _attribute_((_unused_)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
